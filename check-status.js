const {createClient}=require("@supabase/supabase-js");require("dotenv").config({path:".env.local"});async function check(){const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{autoRefreshToken:false,persistSession:false}});console.log("Checklist data:");const{data:d}=await s.from("main_checklist_data").select("status").limit(10);if(d){const u=[...new Set(d.map(i=>i.status))];console.log("Unique statuses:",u);}console.log("
Master codes:");const{data:m}=await s.from("admin_master_code3").select("subcode_name").eq("group_code","GROUP002").eq("is_active",true);if(m){console.log("Subcode names:",m.map(c=>c.subcode_name));}}check();